

import 'package:flutter/material.dart';

class Callus extends StatelessWidget {
  const Callus({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('اتصل بنا'),
      ),
      body: const Center(child: Text('صفحة اتصل بنا')),
    );
  }
}